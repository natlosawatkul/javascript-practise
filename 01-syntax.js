// valiable name can be Hyphens (last-name), underscore (first_name), upper camel(FirstName), lower camel(fisrtName)
//JavaScript is Case Sensitive
// The variables lastName and lastname, are different variables:
let firstName = 'Natmanee'
let lastName = 'Losawatkul'

// JavaScript Operators
// using operators (=) assign values to variables:
let x = 5
let y = 6
let sum = x * y
console.log(sum)
console.log(firstName +" "+ lastName)

let a = "something wrong"
let A = "something upper"
console.log(A)